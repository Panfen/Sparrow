{
  "compilerOptions": {
    "baseUrl": "./",  // all paths are relative to the baseUrl
    "paths": {
        "src/*" : ["src/*"] 
    },
    "jsx": "react",
    "experimentalDecorators": true
  },
  "exclude": [
    "node_modules"
  ]
}
